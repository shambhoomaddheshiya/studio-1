rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Broadly allow authenticated access to all collections for the prototype phase
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // Deployment trigger for rule propagation: 2025-02-17T16:15:00Z
  }
}